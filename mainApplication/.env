# 通用默认配置（会被 .env.development / .env.production / .env.staging 覆盖）
VITE_APP_TITLE=后台管理系统
VITE_APP_ENV=development

# 兼容旧配置与新配置：request.ts 当前使用 VITE_API_BASE
VITE_APP_BASE_API=/api
VITE_API_BASE=/api

# 子路径部署时填写，须以 / 结尾，例如 /admin/
# VITE_PUBLIC_PATH=/
